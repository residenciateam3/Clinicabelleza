---
const items = [1,2,3,4,5];
---
<section class="py-10">
  <p class="text-center text-gray-500 mb-6">Clientes y colaboradores</p>
  <div class="grid grid-cols-2 md:grid-cols-5 gap-6 items-center justify-items-center">
    {items.map(i => (
      <img src={`/images/logos/logo-${i}.webp`} alt={`Logo ${i}`} width="160" height="80" loading="lazy" class="grayscale opacity-70 hover:opacity-100 transition" />
    ))}
  </div>
</section>
